import React from "react";

interface Props {}

interface State {}

export default class Error extends React.Component<Props, State> {
  render() {
    return (
        <div>
            Error
        </div>
    );
  }
}
